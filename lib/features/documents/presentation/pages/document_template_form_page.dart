
import 'package:flutter/material.dart';

class DocumentTemplateFormPage extends StatelessWidget {
  const DocumentTemplateFormPage({super.key, this.id});
  final String? id;
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Placeholder')),
    );
  }
}
