
import 'package:flutter/material.dart';

class BlockchainContractDetailPage extends StatelessWidget {
  const BlockchainContractDetailPage({super.key, this.id});
  final String? id;
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Placeholder')),
    );
  }
}
