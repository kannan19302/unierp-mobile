
import 'package:flutter/material.dart';

class BlockchainTransactionDetailPage extends StatelessWidget {
  const BlockchainTransactionDetailPage({super.key, this.id});
  final String? id;
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Placeholder')),
    );
  }
}
