
import 'package:flutter/material.dart';

class PosDiscountListPage extends StatelessWidget {
  const PosDiscountListPage({super.key});
  
  static const String routeName = 'pos-discounts';
  static const String routePath = '/pos/discounts';
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Placeholder')),
    );
  }
}
