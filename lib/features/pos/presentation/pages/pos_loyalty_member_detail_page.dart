
import 'package:flutter/material.dart';

class PosLoyaltyMemberDetailPage extends StatelessWidget {
  const PosLoyaltyMemberDetailPage({super.key, this.id});
  final String? id;
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Placeholder')),
    );
  }
}
