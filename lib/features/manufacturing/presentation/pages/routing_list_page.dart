
import 'package:flutter/material.dart';

class RoutingListPage extends StatelessWidget {
  const RoutingListPage({super.key});
  
  static const String routeName = 'routings';
  static const String routePath = '/manufacturing/routings';
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Placeholder')),
    );
  }
}
